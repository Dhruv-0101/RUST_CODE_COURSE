fn main() {
    // usize
    // isize
    let days: usize = 55;
    let years: isize = -15_000;
}
//platform depenedent
//means 32 bit computer system then usize is 32 bit and 64 bit then usize is 64 bit
